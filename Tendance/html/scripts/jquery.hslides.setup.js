// Override default settings
$((function () {
    $('#accordion').hSlides({
        totalWidth: 1010,
        totalHeight: 300,
        minPanelWidth: 111,
        maxPanelWidth: 566,
		easing: "easeOutBack",
		activeClass: 'current'
    });
}));